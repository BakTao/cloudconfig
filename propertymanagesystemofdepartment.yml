spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/property
    username: root
    password: 123456
  jackson:
    date-format: yyyy-MM-dd
    time-zone:  GMT+8

mybatis:
  mapper-locations:
  - classpath*:mappers/**/*.xml
  type-aliases-package: com.property.pojo

logging:
  level:
    com.property.dao: debug